#https://wiki.debian.org/CreatePackageFromPPA

sudo apt-get install software-properties-common
sudo apt-add-repository ppa:swi-prolog/devel

# Edit so it points to trusti insead of jessie && keep deb-src line only

sudo apt-get update

 sudo apt-get install libedit-dev libjpeg62-turbo-dev libx11-dev libxpm-dev \
libxt-dev x11proto-core-dev unixodbc-dev default-jdk junit libxft-dev \
libxext-dev libice-dev libxinerama-dev libarchive-dev libunwind8-dev \
libossp-uuid-dev libdb-dev dh-autoreconf


apt-get source --build swi-prolog
